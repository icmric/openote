# Openote v0.3 — the student release plan

> **Document status:** Draft v0.1 · Planning · 2026-08-03
> **Owner:** Eric · **Related:** [v0.2 release plan](v0.2-release-plan.md) (its carried-verification list applies) · [ROADMAP](../../ROADMAP.md) · [PRD](../02-product-requirements.md) · [Vision §6 personas](../00-product-vision.md)
> **Sizes:** **S** ≈ hours · **M** ≈ 1–3 days · **L** ≈ ~a week · **XL** ≈ multi-week, for one developer with AI sessions.

## 1. The strategic reframe

The primary demographic is **students**. That sharpens the vision's three personas into one person: someone who sits in lectures, annotates slides, writes equations, photographs whiteboards, revises for exams, shares notes with classmates, and owns a mix of a laptop and (maybe) a tablet.

The uncomfortable truth this plan is built on: **"OneNote, but open source" is not a reason for most students to switch.** Openness and Linux support win us the enthusiast minority. To win ordinary students, two things must both be true:

1. **Parity where it hurts** — every high-frequency thing OneNote does must work, because a student who hits a missing basic in week one goes back.
2. **A study advantage OneNote cannot match** — features that measurably help *studying*, not just note-keeping. OneNote is a filing cabinet; the wedge is to be the notebook that actively helps you learn from what you wrote.

**Proposed headline for 0.3:** *"Annotate your lecture slides. Tag what matters. Your revision cards make themselves."*

Every differentiator below is chosen against one test: *does this help a student the week before an exam?*

## 2. Current state (as of 2026-08-03, `release/v0.2` @ `477f172`)

**Working and tested** (172 Dart + 29 Rust tests; CI green on Windows/macOS/Linux): freeform canvas (pan/zoom/cull, click-anywhere, snap-to-grid), pressure ink with area + whole-stroke erasers and stylus-conditional palm rejection, math blocks (linear → LaTeX), images/attachments, native + Markdown tables, code blocks, live-Markdown text with **spell check (EN)** and bundled Inter/JetBrains Mono, **tags** (nine kinds, gutter markers, find-tags rollup), notebook-wide search, page outline, favourites/recents, section sort, six built-in templates, backlinks, version history, recycle bin, **two-device sync over any dumb synced folder** (manual pull), the frozen open format + op log, OneNote `.one`/`.onepkg` import at high fidelity, Markdown import, MD/PDF/folder/InkML/JSON-Canvas export, tag-triggered release packaging for three OSes.

**Assumed working, not verified** (carried from the [v0.2 checklist](v0.2-release-plan.md#carried-verification-debt--check-before-or-shortly-after-tagging)): the re-imported reference notebook beyond a quick pass, macOS/Linux builds in human hands, touch hardware, image-`y`, rebuild-from-log on real data.

**Interim seams that shape what's cheap vs expensive below:** text is an opaque Markdown string per block (no per-run styling → no per-run font size, no paragraph collapse); PDF export is raster; sync is pull-on-click; one editor engine behind the ADR-0004 seam.

## 3. Parity — what a switching student still hits

Ordered by how often a student hits the gap in a normal week. "Freq" = how many times per week a typical student would feel it.

### 3.1 Ability gaps

| # | Gap | Freq | Size | Notes |
|---|---|---|---|---|
| P1 | **Paste / drag-drop an image** | Daily, many times | M | Screenshots of slides are *the* student capture flow. Needs `pasteboard` + `desktop_drop` (MIT/BSD — audit before adding). The single most-felt gap in the app. |
| P2 | **JPEG images in OneNote import** | Once, but fatal | M | The importer recovers only PNG. Student notebooks are full of phone photos of whiteboards/worksheets — **a switcher's photos silently vanish on import.** Arguably data loss; belongs at the top. EMF can wait. |
| P3 | **Hyperlinks in OneNote import** | Once, then weekly | S–M | URLs in imported notes are currently dropped; students' notes link to readings and Moodle/Canvas pages. |
| P4 | **Vector (searchable) PDF export** | Weekly | M–L | Students *share notes as PDFs*. Ours are raster screenshots — unsearchable, huge, ugly on zoom. Text + ink drawn into the `pdf` package's canvas as vectors; images embedded. Also the foundation for printing. |
| P5 | **Drag-to-reorder pages/sections** | Weekly | M | Menu-driven today; the navigator already has drag targets for re-parenting, so this extends an existing gesture. |
| P6 | **Height/corner resize + ink scaling** | Weekly | M–L | Width-only today. Scale stroke coordinates on resize (OneNote's behaviour). |
| P7 | **Alignment guides** | Weekly | M | Snap lines against sibling edges while dragging. Group/ungroup proper is the L follow-up; guides alone deliver most of the perceived polish. |
| P8 | **Find-and-replace** | Weekly | S–M | Find exists (page + notebook); replace doesn't. Cheap now that find is real. |
| P9 | **Lasso resize/recolor** | Occasional | M | Gathering/move/delete work; add handles + recolor. |
| P10 | **OneNote tag import** | Once | M | Blocked on a real tagged `.one` fixture + the type-`0x10` walker fix (see v0.2 plan §T.6). The app side is ready for it. |
| P11 | **File attachments in OneNote import** | Once | M | Attached PDFs/docs in imported notebooks currently vanish. Pairs naturally with P2. |
| P12 | **Text alignment (left/centre/right)** | Occasional | M | TEXT-1 residue. Needs a block-level property + toolbar; per-run alignment waits for the text model. |
| P13 | **Print** | Occasional but sharp | S–M after P4 | Students print revision sheets. The `printing` package drives the OS dialog from our (then-vector) PDF. |
| P14 | **Password-protected sections** | Rare | M–L | OneNote has it; students keep journals next to coursework. Real crypto design questions (key from passphrase, what's encrypted at rest, sync interaction) — spec first, don't improvise. |

### 3.2 UI/UX gaps (smaller, but they read as "unfinished")

| # | Gap | Size | Notes |
|---|---|---|---|
| U1 | **Spell-check suggestions menu** | S–M | We underline but offer nothing on right-click. `suggest()` already exists; wire `contextMenuBuilder` + "Add to dictionary" (persist the learned set per workspace — currently session-only). |
| U2 | **Tag keyboard shortcuts** | S | OneNote muscle memory: Ctrl+1 To-Do, Ctrl+2 Important, Ctrl+3 Question. We have the tags; the chords are a switch statement. |
| U3 | **Copy link to page** | S | `[[Title\|id]]` already navigates; add "Copy link" to the page menu so cross-referencing is two clicks. |
| U4 | **Recents/favourites surfaced in the switcher** | S | State + sidebar menu exist; the notebook manager should show them too (planned in the brief, not yet wired there). |
| U5 | **Fence auto-open while typing** (``` ` ``` ``` ` ``` ``` ` ``` + Enter) | S | TEXT-2 residue; a small controller rule. |
| U6 | **Empty-state and first-run polish** | S–M | A student's first 10 minutes: seed the first notebook with a "How Openote works" page built from our own dialect (free, self-documenting). |
| U7 | **Paragraph collapse (fold an outline under its heading)** | blocked | OneNote's collapsible indents. Genuinely useful for long lecture notes, but needs the structured text model — record, don't attempt on the Markdown string. |
| U8 | **Keyboard traversal / a11y pass** (PLAT-5) | M | Tab between blocks, arrow-key nudge selected block, reduced motion. Also simply the right thing. |

## 4. The leg up — student differentiators

Each entry: the pitch in one sentence a student would repeat to a friend, why OneNote can't easily match it, and honest cost.

### D1 — PDF lecture-slide annotation ⭐ *the flagship*

> "Drop the lecture PDF in, write on the slides with your pen, and search the slide text later."

This is the workflow GoodNotes and Notability built businesses on, and it is **Apple-locked and paid** there. OneNote's equivalent ("Insert → PDF printout") rasterises pages into blurry images, slowly, and loses the text. Nobody serves this well on Windows/Linux, and it composes perfectly with what we have: each PDF page becomes a locked background image block on its own Openote page (or stacked on one canvas), ink rides on top exactly as it already does, and the extracted text layer feeds the existing notebook-wide search.

- **Step 1 (spike, S):** PDF rendering. The realistic engine is **pdfium** (BSD-licensed, the Chrome PDF engine). Options: `pdfrx`/`pdfium_bindings` packages, or our own thin FFI in the Rust core (`pdfium-render` crate — but pdfium itself is a prebuilt native binary per platform, ~5 MB; the release workflow must bundle it). Licence-audit whatever we pick.
- **Step 2 (M):** Import flow — "Insert → PDF as pages": render each page at 2× into a content-addressed blob, one Openote page per PDF page (title = `Slides p.3`), background-locked so the eraser/lasso never grab it.
- **Step 3 (M):** Text layer — pdfium text extraction per page stored as hidden `content['sourceText']`, indexed by the existing brute-force search; snippets label themselves "in slide text".
- **Step 4 (M, later):** Re-export annotated PDF (original pages + our ink as vector overlay) for submission/sharing.
- **Total: L–XL**, cleanly staged; steps 1–3 are the 0.3 cut.
- **Risk:** pdfium binary distribution across three OSes (and later Android). The spike answers it before anything else is built.

### D2 — Flashcards from your own notes ⭐ *the study loop*

> "Tag a line as Question or Definition while you take notes; the week before the exam, Openote quizzes you on them."

We accidentally built the on-ramp already: **Question** and **Definition** tags exist, in the gutter, with a rollup panel. The step from "find tags" to "study tags" is small and *no mainstream notes app does it natively* — students maintain a painful second system (Anki/Quizlet) by hand. OneNote structurally can't follow quickly: it has no per-line tag rollup UX this clean and no incentive to cannibalise nothing (there's no Microsoft flashcard product tied to OneNote).

- **Card sources (S–M):** a tagged **Question** line → front; the following indented lines (or rest of the block) → back. A tagged **Definition** line with `term — meaning` or `term: meaning` splits into both directions. `==highlight==` inside a tagged line becomes a cloze deletion.
- **Review mode (M):** a Study panel: deck = current section or notebook, flip-card UI, again/hard/good/easy, **SM-2 scheduling** persisted in workspace settings keyed `notebook:block:line` (survives sync because tags travel with blocks).
- **Anki export (M):** `.apkg` is a zip containing a SQLite database — **we already ship SQLite**; no new dependency. CSV as the trivial fallback. This defuses "but my cards live in Anki": we feed Anki instead of fighting it.
- **Total: L**, partitionable; export alone is a shippable first slice.
- **Scope guard:** this is *reviewing your own notes*, not a flashcard authoring suite. No images-on-cards v1, no shared decks.

### D3 — Math that computes, free

> "OneNote charges your university for math answers. Openote's is free."

OneNote paywalls equation *solving* behind M365 Education subscriptions. We own the linear-math grammar end to end, so **numeric evaluation** of what a student typed is cheap: type `= ` after an expression (or press a button on a math block) → evaluate arithmetic, powers, roots, trig/log, constants, unit-less fractions → append the result. No CAS, no steps, no symbolic algebra in v1 — the honest 80% for first-year problem sets at ~5% of a CAS's cost.

- Evaluator over our existing linear AST in Dart (or Rust, shared with a future CAS), M. Errors show inline and never block rendering.
- The vision's "math evaluation / CAS" future item gets its first concrete step without violating the non-goal ("we do not commit to *solving* them" — evaluation of arithmetic is a calculator, not a solver).
- **Total: M.**

### D4 — Group notebooks over any shared folder *(mostly already true)*

> "Put the notebook in your group's shared Drive folder. Everyone's edits merge. No accounts."

The sync design already makes this structurally work: each member's device appends only its own log, so a shared folder gives N-writer merge with zero conflict dialogs. What's missing is honesty and polish, not architecture: a "Share this notebook…" flow that moves the `.onotebook` into a chosen folder and explains the model; a visible "N people / last pull" surface; auto-pull on window focus (removing the manual click); and a documented caveat that simultaneous edits to the *same paragraph* resolve last-writer-wins until the CRDT lands.

- **Total: M** — extraordinary value for the size, and a genuine "OneNote needs accounts and OneDrive; we need a folder" story. Also the forcing function for the auto-pull (file-watcher) work sync needs anyway.

### D5 — Audio recording pinned to your notes *(the Notability feature)*

> "Record the lecture; tap a line to hear what the lecturer was saying when you wrote it."

Deferred in the PRD (MEDIA-5), but for the student demographic it is a top-three request and OneNote's own version is Windows-only and poor. Requires: recording (a maintained package or platform channels — dependency-light options need auditing), timestamps on block/stroke creation (we already store `strokeStart` and `updatedAt` — most of the correlation data exists), playback with tap-a-block-to-seek, audio blobs in the content-addressed store (sync rides free; large-file caveats to document).

- **Total: L.** Recommended as the *second* flagship, after D1 proves the heavy-asset pipeline (blobs, size, sync) on PDFs.

### D6 — OCR: search your photos *(investigate, then decide)*

> "Search finds the whiteboard photo from week 3."

Composes with P2 (JPEG import) and D1's hidden-text pattern. The engine question decides everything: platform OCR (Windows `Windows.Media.Ocr`, macOS Vision) is free and good but per-platform FFI work and leaves Linux out; Tesseract is cross-platform but heavy (native lib + language data) and mediocre on handwriting. **Spike S, decision after.** Not committed for 0.3.

### D7 — Android tablet build *(reach)*

> "OneNote-grade pen notes on the cheap tablet you actually own."

Flutter makes it plausible; the real costs are Rust-on-Android (NDK build wiring in CI), storage/permissions, and a top-toolbar-on-touch UX pass. The student market on Android tablets is underserved (GoodNotes is iPad-first; OneNote Android is weak). **Total: L–XL, beta target.** iOS stays blocked (AGPL/App Store conflict + no Apple account) — recorded, not forgotten.

## 5. Recommended shape of v0.3

Three phases, each independently shippable; the release can cut after any of them.

**Phase A — "no reason left to go back" (parity sprint):**
P1 paste/drag-drop → P2 JPEG import → P3 hyperlinks → P4 vector PDF export → P5 reorder → P8 find-replace → U1 suggestions → U2 tag chords → U3 copy-link → U5 fences → P7 guides → P6 resize → P9 lasso. (Roughly 3–4 weeks of focused work; front-loaded with the daily-frequency items.)

**Phase B — the flagship:** D1 PDF slide annotation, steps 1–3. This is the feature the release is named for.

**Phase C — the study loop:** D2 flashcards (export slice first, then review mode) + D3 math evaluation + D4 group-notebook polish.

**Stretch (only if C lands early):** D5 audio, D6 OCR spike, P10/P11 importer round (needs your tagged-`.one` fixture), U8 a11y pass.

**Explicitly not in 0.3:** live embeds (still the 0.4 flagship candidate — it composes beautifully with D1/D2 but must not dilute this release), real-time collaboration, Loro/structured text model (unless paragraph-collapse demand forces it), iOS, web clipper, ink recognition, any AI feature, task-manager creep (due-date/agenda views were considered and **cut** — a date on a tag is a slippery slope into a planner; revisit only on real user demand).

## 6. What "nailing OneNote" means as a bar

A concrete acceptance test for the parity claim, student edition — run it before calling Phase A done:

1. Screenshot a slide → paste it into a page → write on it in pen → move both together. *(P1, existing ink)*
2. Import a real student `.onepkg` containing phone photos → the photos are there. *(P2)*
3. Ctrl+1 a line during a lecture; find it in Find Tags that evening. *(U2, existing)*
4. Export a handwritten+typed page to PDF; a classmate searches text in it. *(P4)*
5. Reorder a semester's pages by dragging; nothing mis-nests. *(P5)*
6. Misspell "definately"; right-click; fix it. *(U1)*

## 7. Verification debt carried into this plan

From the [v0.2 checklist](v0.2-release-plan.md#carried-verification-debt--check-before-or-shortly-after-tagging), still open: macOS/Linux human runs · touch hardware · image-`y` PDF export · rebuild-from-log on the real notebook · thorough import verification (quick pass done 2026-08-03, assumed working). Plus new debt this plan will create: pdfium licence/binary audit before D1 step 2, and package audits (`pasteboard`, `desktop_drop`, any audio/recording package) against LICENSING.md before adoption.

---

## Implementation status — 2026-08-03

**225 Dart + 31 Rust tests pass; analyzer clean; `clippy -D warnings` clean.** Five commits on `release/v0.2`.

| Item | State |
|---|---|
| P1 paste / drag-drop | **Done** |
| P2 JPEG import | **Done** — with a marker-walking scanner, because `FF D8`/`FF D9` both occur inside compressed data |
| P8 find-and-replace | **Done** |
| U1 spell suggestions + add-to-dictionary | **Done**, learned words now persist |
| U2 tag chords · U3 copy link · U5 fences | **Done** |
| D2 flashcards | **Done** — cards, SM-2 review, Anki TSV export, study panel with a due badge |
| D3 maths evaluation | **Done** — calculator, not a CAS; an equation is refused rather than guessed at |
| D4 group notebooks / cloud sync | **Done and extended** — folder detection for six providers, non-destructive move, auto-pull on foreign change, one setup dialog. See [ADR-0006 §8](../adr/ADR-0006-sync-transport-and-text-model.md) for why no OAuth |
| **P3 hyperlinks import** | **Not done** — needs the property IDs verified against a real file, same blocker as tag import |
| **P4 vector PDF export** | **Not done** — still a raster capture |
| P5 drag-to-reorder | **Done** — edge thirds reorder, middle nests, subpages travel with their parent |
| P6/P7/P9 canvas polish | **Done** — corner/bottom resize with ink scaling, alignment guides, lasso recolour |
| **Phase B — PDF slide annotation** | **Done (core).** pdfium risk resolved: the tree is MIT, pdfium itself BSD-3-Clause, and a real release build bundles `pdfium.dll` (5.6 MB) beside the exe with the CMake hook intact. Each PDF page becomes a page with a locked background and a searchable hidden text layer. **Step 4 (re-export the annotated PDF) is still to do**, as is verifying the render on macOS/Linux |

### Deliberately not attempted

Phase B was left for a focused pass rather than bolted on at the end of a long session — it is the release's headline and the only piece whose *dependency* could fail cross-platform, which is exactly the kind of thing that should not be rushed.

---

## Field-report pass — 2026-08-03 (second session)

First real use of the Phase A/B/C build produced a list of things that were
broken or half-built rather than missing. All of it is now done: **279 Dart +
38 Rust tests pass, analyzer clean, `clippy -D warnings` clean, and a Windows
release build links the rebuilt core.**

| Reported | Diagnosis | State |
|---|---|---|
| "PDF import isn't working, something about a bad cache directory" | `pdfrx` wires `Pdfrx.getCacheDirectory` from `pdfrxFlutterInitialize()` or from a pdfrx *widget's* initState. We drive the document API directly and never build one, so the first import threw before pdfium was touched | **Fixed** — one call in `main()`, on the root isolate, before `runApp` |
| Imported hyperlinks appear as `<U+FDDF>HYPERLINK "…"` and can't be followed | A OneNote link is not a property — it is a Word **field** inside the paragraph's own text buffer. Nothing filtered it | **Fixed** — conversion at the run level (after the style split, or the 0x1E12 offsets misalign), using run styling to find the display text on whichever side of the instruction it sits. Emitted naked, because the app's inline parser is non-recursive. Bare URLs autolink too |
| — (found while fixing the above) | Fixing the importer does nothing for notes already imported | **Fixed** — `onote_core_repair_field_codes` over FFI; a page heals itself the first time it is opened. One substring test on a clean page |
| "once I have completed a flash card it seems like I can never do it again" | Textbook SM-2's one-day floor: Again scheduled the card for *tomorrow* | **Fixed** — sub-day learning steps, plus a Practice mode that ignores the schedule and never writes it |
| "nothing due" dead-ends; buttons unclear and far apart on big screens; answer reveal unclear | Four dead ends and a stretched layout | **Fixed** — "All caught up, next card in 6h", a real session summary with "review those 3 again", a deck picker (page/section/notebook), a card surface with a progress bar, grade buttons showing what they will do, Space and 1–4 |
| "it needs to be easier to create them" | Tagging produced silence when the line's shape was wrong | **Fixed** — a Make card button that reads the line, picks the tag, and says what it did |
| "I can't type into this box with the image" | Every insertion path made a standalone image block: no text buffer, no editor, higher z, opaque | **Fixed** — a drop or paste onto a text box splices `![](sha256:…)` into its text. Being text is the point: select/cut/move/paste need no new code |
| "if I drag it over an existing box, I want to put it into that box at that point" | Same root cause, plus a coordinate bug: `desktop_drop` already hands over a local position and the canvas converted it again | **Fixed** |
| "when I hover over a box, left click and move my mouse, this is me trying to highlight text, NOT move the box" | One GestureDetector wrapped the whole block | **Fixed** — OneNote's move bar. Body drags select text from the *first* gesture; clicking places the caret where you clicked. Alt-drag and long-press are the escape hatches |
| "the whole app is super slow" | Three per-keystroke whole-notebook reads behind the command bar and status bar | **Fixed** (previous session) — cached, with wall-clock regression guards |
| "after selecting one it still shows up as greyed out… clicking it opens the same menu" | The chip didn't know where the notebook lived, and the dialog had one face | **Fixed** — `SyncStatus`, and a dialog that says what is set up with Open folder / Move elsewhere |
| "maybe we give people the option to mirror the notebook? Backups would also be a good idea" | The mechanism ran on every save but had no UI | **Fixed** — mirrors and dated backups, both one-way, configurable per notebook |
| "we should make the onboarding process easy" | Nothing existed | **Fixed** — a welcome flow that *finds* notebooks already in your cloud folders, plus OneNote export instructions (the step people get stuck on) and "just start writing" |

### Still open

- **P4 vector PDF export** — still a raster capture.
- **Phase B step 4** — re-export an annotated PDF.
- **P10 OneNote tag import** — needs a tagged `.one` fixture and the `0x10` walker fix.
- **Hyperlink layout is inferred, not measured.** There is no committed `.one`
  fixture, so the parser handles the display text on *either* side of the
  instruction rather than assuming one. Worst case is a slightly-off label,
  never lost text — but a real file would let us settle it.
- Carried debt unchanged: macOS/Linux human runs · touch hardware ·
  image-`y` PDF export · rebuild-from-log on the real notebook.

---

## Second field-report pass — 2026-08-04

A real `.one` file arrived (`Natural Language and Intro to Truth Tables.one`),
which settled the last inferred thing in the importer, and a code survey ahead
of the merge to `main` turned up four data-safety problems that no test was
watching. **291 Dart + 40 Rust tests pass, analyzer clean, `clippy -D warnings`
clean, Windows release build links the rebuilt core.**

### Reported

| Reported | Diagnosis | State |
|---|---|---|
| Hyperlink label still wrong | Measured at last. The buffer is `Video: <U+FDDF>HYPERLINK "…"Natural Language and Introduction to Truth Tables`, with 0x1E12 boundaries at 7 and 63 — so the label FOLLOWS the instruction and is its own run. The trap: OneNote gives the invisible instruction characters the *surrounding prose's* formatting (both bold here) and the label the link's, so matching by "same style as the instruction" stops before the label and then walks back into the sentence — which is exactly how it came out as `[Video:](url)Natural Language…` | **Fixed** — the run boundary at the instruction's end is the signal. Verified end-to-end against the file |
| "PDF import … place every page consecutively on the current page but below the last box" | It made one page per slide | **Done** — a printout stacked down the current page is now the default; one-page-per-slide is behind the button's arrow |
| "when i click on a box it usually puts my cursor right at the end" | `_editableState()` searched DOWNWARDS from the focus node's context for `EditableTextState`. `EditableText` builds its `Focus` *inside* itself, so the state is an ANCESTOR — the search returned null every time, and `EditableText` fell through to its documented "place cursor at the end if the selection is invalid when we receive focus" | **Fixed** — walk up. Independently confirmed by two reviewers, one of which ran a probe test proving the direction |
| "highlighting from unselected box doesnt seem to be working" | Same single root cause: `_selectBase ??= session.offsetAtGlobal(from)` bailed on the same null | **Fixed by the same line** |

### Found in review, fixed before merge

| Problem | Why it mattered | State |
|---|---|---|
| A second device opened the **same WAL SQLite container** out of the cloud folder | ADR-0006 §3 says `cache.onote ← local-only SQLite; never synced`. Two machines writing one copy of it through a file-replicating cloud client is the corruption case the whole log design exists to avoid — and the sync dialog told the student the opposite | **Fixed** — `NotebookRef.logDir` decouples the log directory from the container path. A joining device copies the container into its own workspace and shares only the append-only logs |
| A notebook moved to a cloud folder **vanished from the list on restart** | The registry stored a basename and re-joined it against the workspace folder, so the path resolved to a file that isn't there. The data was never lost; the notebook was, as far as the user could tell | **Fixed** — full path for anything outside the workspace |
| Grading a card **deleted every other notebook's review history** | Card scheduling is workspace-global; pruning was scoped to one notebook's deck | **Fixed** — pruning only removes entries whose block is demonstrably in the current notebook |
| "Backup" contained **no notebook** | The mirror source is the `.onotebook` directory and the container is its *sibling*, so the in-walk `.onote` skip was dead code | **Fixed** — the container is passed in and copied into backup snapshots |
| Tag line indices **never re-based** | A tag records a line index; pressing Enter above a tagged line moved the marker onto the wrong sentence, and the flashcard followed it while its id — `blockId:line` — stayed the same, so the schedule stayed attached to a card that was now about something else | **Fixed** — `NoteTag.rebase` on every edit that changes the line count |
| README/CHANGELOG described a different product | "single-device, local-only MVP", "Loro not yet wired", and a conflict-freedom claim that is true of the logs but was not true of the container | **Fixed** |

### Found in an adversarial review of the above, also fixed

Twenty-eight agents over four lenses, each finding then handed to a verifier
told to refute it. Nineteen survived; the ones that mattered:

- **`syncStatus` and `moveNotebookTo` both bypassed the new `logDir`**, so a
  device that had just joined read "not syncing" and moving that notebook
  looked for a log directory that was never there. Both now go through one
  `NotebookRef.logDirPath` accessor.
- **Trashed notebooks got only half the registry fix** — a notebook deleted
  after being moved to a cloud folder was still written as a bare basename,
  dropped at the next load, its file orphaned. The 30-day retention promise
  quietly became "until you restart".
- **`NoteTag.rebase` renamed flashcards.** A card is `blockId:line`, so moving
  a tag renames its card and orphans the schedule — which the new scoped prune
  then deleted. Rebase now reports where each tag went and the schedule travels
  with it.
- **A rewritten block of lines relocated its tags** onto lines they never
  marked. Only a *pure* insertion shifts them now; anything else drops them.
- **The PDF printout could land on top of existing notes.** `contentExtent`
  substituted a flat 60px for any block the canvas had culled, which
  *under*-reports the bottom edge — the opposite of what the comment claimed.
  It now estimates from the block's own text, which helps page growth and
  fit-to-content too.
- **The hyperlink label ran past its own run** into the following prose, and
  the backward fallback treated OneNote's automatic black (`FF000000`) as "a
  colour", so it still ate the sentence. 75 of the 78 styled runs in the real
  file are automatic black, so the guard was inert on realistic input and the
  test that "proved" it passed only because its fixture used colour 0.
- **A backup copied a live WAL database behind SQLite's back**, so it could
  capture one missing the last several saves. It now asks SQLite for a
  consistent copy (`VACUUM INTO`).
- **The repair path discarded `onenote:`/`file:` link targets** — fine as a
  rendering choice at import, unrecoverable data loss when rewriting notes
  imported months ago. The address is kept as text. The repair is also
  undoable now, with the checkpoint taken *before* the rewrite.
- Imported slides had no unlock affordance (now in the block menu), the
  printout scrolled nowhere so the app reported success while nothing visibly
  changed, and a stale caret token from a table/code/equation click could
  hijack the next text box.

### Known and accepted

**Clicking into a line with Markdown markers places the caret approximately.**
The click happens on the read-only view, where markers are gone, and is
resolved against the edit view, where they are present. Plain lines are exact.
Teaching `MarkdownView` to report source offsets is a much larger piece of
machinery than the error justifies — noted in the code so it is not refiled as
the "caret at the end" bug, which was a different thing entirely.

### Still open, in rough priority order

1. **Demote the container to `cache.onote`** — the remaining half of ADR-0006 §3.
   A joining device is now safe, but the *first* device's container still sits
   in the synced folder being re-uploaded after every save. Wasteful, not
   corrupting.
2. **Blob garbage collection.** Nothing ever deletes a blob, and blobs are
   stored twice (SQLite table + loose file). A 60-slide deck at 2× render is
   ~120 MB, permanently, and PDF import makes this the fastest way to fill a
   disk.
3. **Vector PDF export**, then printing. Export is a raster capture, so an
   exported page is not searchable — the one remaining daily-frequency parity
   gap, and the blocker on the Phase A acceptance test.
4. **macOS/Linux human runs**, including one PDF import per platform (the
   committed macOS plugin registrant does not mention pdfium; Flutter may
   regenerate it at build time, but it is cheap to settle and the flagship
   depends on it).
5. **Smoke tests for the surfaces with none**: `pdf_import`, `study_panel`,
   `sync_dialog`, `onboarding`.
6. Field-code repair on page open still has no `pushUndo`.
7. P10 OneNote tag import — the newly-arrived `.one` file has no tags, so this
   still wants a tagged fixture.
8. Touch hardware; image-`y` PDF export; rebuild-from-log on a real notebook.

---

## Third pass — 2026-08-04 (post-merge review + fixes)

A [full code review of the v0.2 merge](../reviews/2026-08-code-review-v02-merge.md)
plus two reported bugs. **309 Dart + 41 Rust tests pass; analyzer clean;
`clippy -D warnings` clean.**

### Reported

| Reported | Diagnosis | State |
|---|---|---|
| "I am at the moment unable to select the pen or anything in the menu" | `_drawRow` returned a `Row` containing a **`Spacer`**, and every command row is built inside a horizontal `SingleChildScrollView` — i.e. under an *unbounded* width constraint, where a flex child is a hard layout assertion. The whole Draw row therefore failed to lay out: no pen, no highlighter, no eraser, nothing clickable. Nothing about the symptom pointed at layout, which is why it read as "the buttons are dead" | **Fixed** — fixed-width gap instead. Three widget tests now tap every ink tool through the real bar |
| — (found while fixing the above) | The **tab row** overflowed by 18 px at 560 px wide. An overflowing `Row` is *clipped*, and clipped pixels do not hit-test, so on a laptop with the navigator open the right-hand buttons silently stopped responding | **Fixed** — the trailing cluster is `Flexible` + a reversed horizontal scroll, so it shrinks before the tabs do and keeps its right-hand end visible |
| "it will print each page of the PDF to its own new page… I want it to insert onto the current page either at the cursor or below the last box, every PDF page on the one single Openote page" | Two things. The printout path was already correct **but** `importPdfFile` silently switched to one-page-per-slide whenever `app.pageId == null` — and opening the app on a section rather than a page is ordinary, so the wrong mode was reachable without choosing it. Separately, insertion was always "below everything", never at the cursor | **Fixed** — no page open now *creates* one and stacks onto it; insertion anchors on the block being edited (or selected) and **pushes what is below it down**, so a mid-page import opens a gap instead of burying notes. Four tests |

### From the review — fixed

- **A foreign change arriving mid-pull was dropped.** `syncPull` returned early
  on re-entrancy, but the watcher's debounce had already fired, so nothing was
  going to ask again: the other device's edits sat unapplied until some
  unrelated later change happened to fire the watcher — possibly never. Now a
  `_pullAgain` flag drives a loop, keeping the original guarantee (no two
  concurrent pulls, so no double-apply) while adding liveness. Both properties
  have a test.
- **Vector PDF export** — the last daily-frequency parity gap. Text is emitted as
  real text in the bundled Inter face (embedded subset + `/ToUnicode` CMap, so
  Ctrl+F and copy-paste work in any reader), ink as stroked PDF paths, images
  embedded, and a tall page **paginates** into sheets instead of being cut off.
  It is also the foundation for **printing (P13)** and **annotated-slide
  re-export (Phase B step 4)**, which now share `buildPagePdf`. The raster
  capture stays in the menu as "picture of the page". Seven tests.

### Deferred, with reasons

- **Blob de-duplication** was on the list and is *not* a standalone change:
  SQLite is what the app reads and `.onotebook/blobs/` is what sync replicates,
  so removing either breaks a real path. De-duplication **is** the ADR-0006
  container demotion, and GC needs an ADR of its own because a blob is
  referenced by pages, history snapshots *and* other devices' logs that may not
  have synced yet. Both are now item E1/E2 of [v0.4](v0.4-and-beyond.md).
- **The `AppState` split** (E3) — mechanical but large; scheduled ahead of v0.4
  features so they don't land in the god object too.

### Product backlog

The review's product suggestions now live in
**[v0.4-and-beyond.md](v0.4-and-beyond.md)** rather than in a review document:
study stats + exam countdown, share-as-PDF, dark slides, page thumbnails, the
positive import report, audio-pinned notes, OCR, OneNote tag import, password
sections — each with a size, a reason, and what blocks it.
